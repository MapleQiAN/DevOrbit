# Stage 4: 前端项目初始化与登录流程 - 完成总结

## 🎯 阶段目标

✅ **已完成**

- 初始化 Vue 3 + TypeScript + Vite 项目
- 配置 Vue Router、Pinia、Axios
- 实现登录页面和 OAuth 回调处理
- 实现认证状态管理

---

## 📁 创建的文件清单

### 项目配置文件

| 文件 | 说明 |
|------|------|
| `frontend/package.json` | 项目依赖和脚本 |
| `frontend/vite.config.ts` | Vite 构建配置 |
| `frontend/tsconfig.json` | TypeScript 配置 |
| `frontend/tsconfig.node.json` | Node.js TypeScript 配置 |
| `frontend/index.html` | HTML 入口文件 |

### 源代码文件

#### 核心文件

| 文件 | 说明 |
|------|------|
| `frontend/src/main.ts` | 应用入口 |
| `frontend/src/App.vue` | 根组件 |

#### API 层

| 文件 | 说明 |
|------|------|
| `frontend/src/api/client.ts` | Axios 客户端配置 |
| `frontend/src/api/auth.ts` | 认证 API 调用 |
| `frontend/src/api/github.ts` | GitHub API 调用 |

#### 状态管理

| 文件 | 说明 |
|------|------|
| `frontend/src/stores/auth.ts` | 认证状态管理（Pinia） |

#### 路由和视图

| 文件 | 说明 |
|------|------|
| `frontend/src/router/index.ts` | 路由配置和守卫 |
| `frontend/src/views/Login.vue` | 登录页面 |
| `frontend/src/views/GithubCallback.vue` | OAuth 回调页面 |
| `frontend/src/views/Dashboard.vue` | 仪表盘页面（占位符） |

---

## 🔧 实现的功能详解

### 1. 项目配置

#### Vite 配置 (`vite.config.ts`)

**特点**:
- Vue 3 插件支持
- 路径别名配置（`@` 指向 `src`）
- 开发服务器配置（端口 5173）
- API 代理配置（`/api` 代理到后端）

**代理配置**:
```typescript
proxy: {
  '/api': {
    target: 'http://localhost:8000',
    changeOrigin: true,
    rewrite: (path) => path.replace(/^\/api/, '')
  }
}
```

#### TypeScript 配置 (`tsconfig.json`)

**特点**:
- 严格模式启用
- 路径映射配置
- Vue 3 类型支持
- 未使用变量检查

### 2. API 客户端 (`src/api/client.ts`)

**功能**:
- 创建 Axios 实例
- 自动添加 Authorization header
- 处理 401 错误（token 过期）
- 统一的超时配置

**请求拦截器**:
```typescript
client.interceptors.request.use((config) => {
  const authStore = useAuthStore()
  if (authStore.token) {
    config.headers.Authorization = `Bearer ${authStore.token}`
  }
  return config
})
```

**响应拦截器**:
```typescript
client.interceptors.response.use(
  (response) => response,
  (error) => {
    if (error.response?.status === 401) {
      // token 过期，清除认证并重定向到登录
      authStore.logout()
      window.location.href = '/login'
    }
    return Promise.reject(error)
  }
)
```

### 3. 认证 API (`src/api/auth.ts`)

**导出的函数**:

#### `getGithubAuthorizationUrl(): Promise<string>`

获取 GitHub OAuth 授权 URL

```typescript
const authUrl = await getGithubAuthorizationUrl()
window.location.href = authUrl
```

#### `handleGithubCallback(code: string): Promise<LoginResponse>`

处理 GitHub OAuth 回调

```typescript
const response = await handleGithubCallback(code)
// response.access_token: JWT token
// response.user: 用户信息
```

### 4. GitHub API (`src/api/github.ts`)

**导出的函数**:

#### `syncGithubData(): Promise<GithubSyncResponse>`

同步 GitHub 数据

```typescript
const result = await syncGithubData()
// result.repos_count: 仓库数
// result.stats_updated: 更新的统计记录数
```

#### `getDailyStats(fromDate?: string, toDate?: string): Promise<GithubDailyStatsResponse>`

获取每日统计数据

```typescript
const stats = await getDailyStats('2025-11-09', '2025-12-09')
// stats.data: 每日统计数据数组
// stats.total: 总记录数
```

### 5. 认证状态管理 (`src/stores/auth.ts`)

**使用 Pinia 的状态管理**

**状态**:
- `token`: JWT token
- `user`: 用户信息

**计算属性**:
- `isAuthenticated`: 是否已认证

**方法**:
- `setAuth(token, user)`: 设置认证信息
- `logout()`: 清除认证信息

**使用示例**:
```typescript
import { useAuthStore } from '@/stores/auth'

const authStore = useAuthStore()

// 检查是否已认证
if (authStore.isAuthenticated) {
  console.log('已登录')
}

// 获取当前用户
console.log(authStore.user)

// 设置认证信息
authStore.setAuth(token, user)

// 退出登录
authStore.logout()
```

**持久化**:
- 自动保存到 localStorage
- 页面刷新时自动恢复

### 6. 路由配置 (`src/router/index.ts`)

**路由表**:

| 路径 | 组件 | 认证 | 说明 |
|------|------|------|------|
| `/` | - | - | 重定向到 `/dashboard` |
| `/login` | Login.vue | ❌ | 登录页面 |
| `/auth/github/callback` | GithubCallback.vue | ❌ | OAuth 回调页面 |
| `/dashboard` | Dashboard.vue | ✅ | 仪表盘页面 |

**路由守卫**:
```typescript
router.beforeEach((to, from, next) => {
  const authStore = useAuthStore()
  const requiresAuth = to.meta.requiresAuth !== false

  if (requiresAuth && !authStore.isAuthenticated) {
    // 需要认证但未登录，重定向到登录页
    next('/login')
  } else if (!requiresAuth && to.path === '/login' && authStore.isAuthenticated) {
    // 已登录但访问登录页，重定向到仪表盘
    next('/dashboard')
  } else {
    next()
  }
})
```

### 7. 登录页面 (`src/views/Login.vue`)

**功能**:
- 显示项目信息
- GitHub 登录按钮
- 调用后端 API 获取授权 URL
- 重定向到 GitHub 授权页面

**样式**:
- 响应式设计
- 渐变背景
- 卡片式布局

### 8. OAuth 回调页面 (`src/views/GithubCallback.vue`)

**工作流程**:

```
1. 页面加载时，从 URL 中提取 code 参数
   ↓
2. 调用后端 /auth/github/callback?code=xxx
   ↓
3. 获取 JWT token 和用户信息
   ↓
4. 保存到 Pinia store 和 localStorage
   ↓
5. 跳转到仪表盘
```

**状态管理**:
- `isLoading`: 加载中
- `error`: 错误信息

**UI 状态**:
- 加载中：显示加载动画
- 错误：显示错误信息和返回按钮
- 成功：显示成功消息并跳转

### 9. 仪表盘页面 (`src/views/Dashboard.vue`)

**当前功能**:
- 显示用户信息（头像、用户名）
- 退出登录按钮
- 功能预览列表

**占位符说明**:
- 这是 Stage 4 的占位符
- 完整的图表和数据展示将在 Stage 5 中实现

---

## 🚀 本地运行验证

### 步骤 1: 安装依赖

```bash
cd frontend
npm install
```

### 步骤 2: 配置环境变量

```bash
# 创建 .env.local 文件
cp .env.example .env.local

# 编辑 .env.local，配置 API 地址
VITE_API_BASE_URL=http://localhost:8000
```

### 步骤 3: 启动开发服务器

```bash
npm run dev
```

你应该看到类似的输出：

```
  VITE v4.5.0  ready in 123 ms

  ➜  Local:   http://localhost:5173/
  ➜  press h to show help
```

### 步骤 4: 在浏览器中打开

访问 http://localhost:5173

你应该看到：
1. 如果未登录，重定向到 `/login` 页面
2. 登录页面显示 GitHub 登录按钮
3. 点击按钮后跳转到 GitHub 授权页面

### 步骤 5: 完整的登录流程测试

1. 访问 http://localhost:5173
2. 点击「使用 GitHub 登录」按钮
3. 在 GitHub 授权页面授权
4. 重定向到 http://localhost:5173/auth/github/callback
5. 显示加载动画
6. 自动跳转到 http://localhost:5173/dashboard
7. 显示用户信息和仪表盘

---

## 📊 前端架构图

```
┌─────────────────────────────────────────────────────────────┐
│                    Vue 3 应用                               │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  ┌──────────────────────────────────────────────────────┐  │
│  │              Router (Vue Router)                     │  │
│  │  ┌──────────────┐  ┌──────────────┐  ┌────────────┐ │  │
│  │  │ /login       │  │ /auth/github │  │ /dashboard │ │  │
│  │  │ Login.vue    │  │ Callback.vue │  │ Dashboard  │ │  │
│  │  └──────────────┘  └──────────────┘  └────────────┘ │  │
│  └──────────────────────────────────────────────────────┘  │
│                           │                                 │
│                           ▼                                 │
│  ┌──────────────────────────────────────────────────────┐  │
│  │           State Management (Pinia)                  │  │
│  │  ┌────────────────────────────────────────────────┐ │  │
│  │  │ Auth Store                                     │ │  │
│  │  │ - token                                        │ │  │
│  │  │ - user                                         │ │  │
│  │  │ - isAuthenticated                              │ │  │
│  │  └────────────────────────────────────────────────┘ │  │
│  └──────────────────────────────────────────────────────┘  │
│                           │                                 │
│                           ▼                                 │
│  ┌──────────────────────────────────────────────────────┐  │
│  │              API Layer (Axios)                      │  │
│  │  ┌──────────────┐  ┌──────────────┐  ┌────────────┐ │  │
│  │  │ auth.ts      │  │ github.ts    │  │ client.ts  │ │  │
│  │  └──────────────┘  └──────────────┘  └────────────┘ │  │
│  └──────────────────────────────────────────────────────┘  │
│                           │                                 │
│                           ▼                                 │
│                  Backend API (FastAPI)                      │
└─────────────────────────────────────────────────────────────┘
```

---

## 🔄 完整登录流程

```
用户访问应用
    │
    ▼
路由守卫检查认证状态
    │
    ├─ 已认证 → 跳转到 /dashboard
    │
    └─ 未认证 → 显示 /login 页面
         │
         ▼
    用户点击「GitHub 登录」
         │
         ▼
    调用 getGithubAuthorizationUrl()
         │
         ▼
    重定向到 GitHub 授权页面
         │
         ▼
    用户授权
         │
         ▼
    GitHub 重定向到 /auth/github/callback?code=xxx
         │
         ▼
    GithubCallback.vue 页面加载
         │
         ▼
    提取 URL 中的 code 参数
         │
         ▼
    调用 handleGithubCallback(code)
         │
         ▼
    后端处理 OAuth 流程，返回 JWT token
         │
         ▼
    保存 token 和用户信息到 Pinia store
         │
         ▼
    保存到 localStorage（持久化）
         │
         ▼
    跳转到 /dashboard
         │
         ▼
    显示仪表盘页面
```

---

## ✅ 完成清单

- [x] Vue 3 + TypeScript 项目初始化
- [x] Vite 构建配置
- [x] Vue Router 路由配置
- [x] Pinia 状态管理
- [x] Axios API 客户端
- [x] 认证 API 调用
- [x] GitHub API 调用
- [x] 登录页面
- [x] OAuth 回调处理
- [x] 仪表盘页面（占位符）
- [x] 路由守卫
- [x] 响应式设计

---

## 🎉 总结

**Stage 4 已成功完成！**

现在你有了完整的前端项目：

1. ✅ 项目结构清晰，易于扩展
2. ✅ 认证流程完整
3. ✅ 状态管理完善
4. ✅ API 调用封装好
5. ✅ 路由守卫保护

**前端基础框架已完全搭建！** 🎊

**下一步**：Stage 5 - 前端仪表盘与数据展示

---

**创建时间**: 2025-12-09  
**版本**: 0.1.0 - Stage 4 完成

